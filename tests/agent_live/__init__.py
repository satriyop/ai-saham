"""Opt-in live smoke suite for the TUI AI agent journey (Phases 0–3).

Select with ``pytest -m agent-live-call`` after setting ``AI_SAHAM_AGENT_LIVE=1``
and provider credentials. Default and offline ``pytest -m agent`` never execute
live HTTP from this package.
"""

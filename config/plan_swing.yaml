version: 1

plan_swing:
  auto_refresh:
    market_days: 365
    broker_days: 90

  sentiment:
    max_headlines: 20
    days: 3

  evidence:
    flow_detail_window_sessions: 30

  candidate:
    # Internal candidate construction for single-ticker plan structure.
    # Keep permissive by default so SignalEngine/RiskEngine decide assessment
    # when Action recompute flags are enabled.
    min_net_buy_days: 0
    min_accum_score: 0.0
